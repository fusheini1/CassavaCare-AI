# =============================================================================
# AI-Powered Cassava Disease Diagnosis - Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# Recommended: Use a virtual environment (python -m venv venv)
# =============================================================================
# All versions are pinned exactly (==) to the versions that were installed
# and verified during development, so installs are reproducible.
# Regenerate with: pip freeze (only for the direct dependencies below).
# =============================================================================

# Web Framework
Flask==3.1.3
Werkzeug==3.1.8
Flask-Limiter==4.1.1

# Environment Configuration (reads .env for FLASK_DEBUG, FLASK_HOST, FLASK_PORT)
python-dotenv==1.0.1

# Deep Learning
tensorflow==2.21.0

# Image Processing
Pillow==12.2.0
numpy==2.4.4

# QR Code Generation
qrcode==8.2

# Production WSGI Server (Linux/macOS only)
gunicorn==26.0.0
